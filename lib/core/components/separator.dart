import 'package:flutter/material.dart';

class Separator extends SizedBox {
  const Separator.height(double height, {super.key}) : super(height: height);

  const Separator.width(double width, {super.key}) : super(width: width);
}
